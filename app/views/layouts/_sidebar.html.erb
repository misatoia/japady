        <div class="card">
            <img class="card-img-top" src="<%= gravatar_url(current_user, { size: 200 }) %>" alt=""> 
            <div class="card-body">
              <div class="card-title"><%= current_user.nickname %></div>
              <div class="card-text">
                <div class="border p-2">
                    <%= simple_format(current_user.purpose) %>
                </div>
                <% if !member? %>
                    <div class="text-danger">
                        <p>あなたはこのサービスの正しい利用者としてまだ確認されていません。</p>
                        <p>以下のページをご参照ください。</p>
                        <p><%= link_to 'Japadyについて', about_path %></p>
                    </div>
                <% end %>
            </div>
        </div>

